{
  "name": "hanjul-ide-spike-webapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "한줄 IDE P0 스파이크 — packages/doc mountEditor 를 pywebview 셸 안에서 로드하는 최소 vite 앱. 루트 npm workspaces 에는 편입하지 않는다(독립 실행).",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
